/**
 * Hero portrait — replace `profile.png` with your photo, or change the path below
 * (e.g. `export { default as heroPortrait } from "./my-photo.jpg";`).
 */
export { default as heroPortrait } from "./profile.png";
